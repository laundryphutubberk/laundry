# Delivery Task

Feature ownership layer for dispatch, resort receipt, returned movement, and work closure.

This task consumes shared standards from FE-02 through FE-08.

Status: NOT_STARTED

Primary flow:

Packed → Ready for Delivery → Dispatch → Resort Receives → Confirm Return → Close Work
