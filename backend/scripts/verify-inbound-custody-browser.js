/** * verify-inbound-custody-browser.js * * Browser-based verification for the Laundry Native Inbound Custody V1 feature. * * This script verifies the complete End-to-End implementation: * - Prisma schema and migration * - Backend layers (repository, domain, service, controller, routes) * - Frontend layers (API, policy, controller, runtime panel, detail page integration) * * Usage: node scripts/verify-inbound-custody-browser.js */const fs = require('fs');const path = require('path');const ROOT = path.resolve(__dirname, '..');const BACKEND_ROOT = ROOT;const FRONTEND_ROOT = path.resolve(__dirname, '..', '..', 'frontend');let passed = 0;let failed = 0;function check(description, condition) {  if (condition) {    console.log(`  ✓ ${description}`);    passed++;  } else {    console.log(`  ✗ ${description}`);    failed++;  }}function fileExists(filePath) {  return fs.existsSync(filePath);}function fileContains(filePath, search) {  if (!fileExists(filePath)) return false;  const content = fs.readFileSync(filePath, 'utf-8');  return content.includes(search);}console.log('');console.log('═══════════════════════════════════════════════════════════════');console.log('  Laundry Native Inbound Custody V1 — Browser Verification');console.log('═══════════════════════════════════════════════════════════════');console.log('');// ── 1. Prisma Schema ──────────────────────────────────────────────────────console.log('── 1. Prisma Schema ──────────────────────────────────────────');const schemaPath = path.join(ROOT, 'prisma', 'schema.prisma');check('schema.prisma exists', fileExists(schemaPath));check('schema has InboundCustodyOperation model', fileContains(schemaPath, 'InboundCustodyOperation'));check('schema has InboundCustodyOperation fields', fileContains(schemaPath, 'profile') && fileContains(schemaPath, 'trackingLevel'));check('schema has InboundCustodyOperation status enum', fileContains(schemaPath, 'inboundCustodyStatus') || fileContains(schemaPath, 'PENDING'));check('schema has InboundCustodyOperation version field', fileContains(schemaPath, 'version'));check('schema has InboundCustodyOperation work relation', fileContains(schemaPath, 'workId') && fileContains(schemaPath, 'laundryWork'));// ── 2. Migration ──────────────────────────────────────────────────────────console.log('');console.log('── 2. Migration ──────────────────────────────────────────────');const migrationDir = path.join(ROOT, 'prisma', 'migrations', '20260717120000_add_inbound_custody_operation');check('migration directory exists', fs.existsSync(migrationDir));const migrationSql = path.join(migrationDir, 'migration.sql');check('migration.sql exists', fileExists(migrationSql));check('migration creates InboundCustodyOperation table', fileContains(migrationSql, 'InboundCustodyOperation') || fileContains(migrationSql, 'inbound_custody_operation'));check('migration has version column', fileContains(migrationSql, 'version'));check('migration has foreign key to LaundryWork', fileContains(migrationSql, 'LaundryWork'));// ── 3. Backend Repository ─────────────────────────────────────────────────console.log('');console.log('── 3. Backend Repository ─────────────────────────────────────');const repoPath = path.join(ROOT, 'src', 'repositories', 'inboundCustody.repository.js');check('inboundCustody.repository.js exists', fileExists(repoPath));check('repository exports functions', fileContains(repoPath, 'module.exports'));check('repository has findCustodyByWorkId', fileContains(repoPath, 'findCustodyByWorkId'));check('repository has createCustody', fileContains(repoPath, 'createCustody'));check('repository has updateCustody', fileContains(repoPath, 'updateCustody'));check('repository has transaction support', fileContains(repoPath, 'transaction'));// ── 4. Backend Domain ─────────────────────────────────────────────────────console.log('');console.log('── 4. Backend Domain ─────────────────────────────────────────');const domainPath = path.join(ROOT, 'src', 'domain', 'inboundCustody.business.js');check('inboundCustody.business.js exists', fileExists(domainPath));check('domain exports functions', fileContains(domainPath, 'module.exports'));check('domain has assertValidTransition', fileContains(domainPath, 'assertValidTransition'));check('domain has assertWorkIsInboundEligible', fileContains(domainPath, 'assertWorkIsInboundEligible'));check('domain has assertReceiptCanConfirm', fileContains(domainPath, 'assertReceiptCanConfirm'));check('domain has assertCountEvidenceCanRecord', fileContains(domainPath, 'assertCountEvidenceCanRecord'));check('domain has assertCanClose', fileContains(domainPath, 'assertCanClose'));check('domain has buildCreateData', fileContains(domainPath, 'buildCreateData'));check('domain has buildReceiptConfirmData', fileContains(domainPath, 'buildReceiptConfirmData'));check('domain has buildCountEvidenceData', fileContains(domainPath, 'buildCountEvidenceData'));check('domain has buildCloseData', fileContains(domainPath, 'buildCloseData'));// ── 5. Backend Service ────────────────────────────────────────────────────console.log('');console.log('── 5. Backend Service ────────────────────────────────────────');const servicePath = path.join(ROOT, 'src', 'services', 'inboundCustody.service.js');check('inboundCustody.service.js exists', fileExists(servicePath));check('service exports functions', fileContains(servicePath, 'module.exports'));check('service has getCustody', fileContains(servicePath, 'getCustody'));check('service has initiateCustody', fileContains(servicePath, 'initiateCustody'));check('service has confirmReceipt', fileContains(servicePath, 'confirmReceipt'));check('service has recordCountEvidence', fileContains(servicePath, 'recordCountEvidence'));check('service has closeCustody', fileContains(servicePath, 'closeCustody'));// ── 6. Backend Validator ──────────────────────────────────────────────────console.log('');console.log('── 6. Backend Validator ──────────────────────────────────────');const validatorPath = path.join(ROOT, 'src', 'validators', 'inboundCustody.validator.js');check('inboundCustody.validator.js exists', fileExists(validatorPath));check('validator exports functions', fileContains(validatorPath, 'module.exports'));// ── 7. Backend Controller ─────────────────────────────────────────────────console.log('');console.log('── 7. Backend Controller ─────────────────────────────────────');const controllerPath = path.join(ROOT, 'src', 'controllers', 'inboundCustody.controller.js');check('inboundCustody.controller.js exists', fileExists(controllerPath));check('controller exports functions', fileContains(controllerPath, 'module.exports'));// ── 8. Backend Routes ─────────────────────────────────────────────────────console.log('');console.log('── 8. Backend Routes ─────────────────────────────────────────');const routesPath = path.join(ROOT, 'src', 'routes', 'inboundCustody.routes.js');check('inboundCustody.routes.js exists', fileExists(routesPath));check('routes exports router', fileContains(routesPath, 'Router') || fileContains(routesPath, 'router'));check('routes has GET /custody endpoint', fileContains(routesPath, 'get') || fileContains(routesPath, 'GET'));check('routes has POST /custody/initiate endpoint', fileContains(routesPath, 'initiate'));check('routes has POST /custody/confirm-receipt endpoint', fileContains(routesPath, 'confirm-receipt') || fileContains(routesPath, 'confirmReceipt'));check('routes has POST /custody/record-count-evidence endpoint', fileContains(routesPath, 'record-count-evidence') || fileContains(routesPath, 'recordCountEvidence'));check('routes has POST /custody/close endpoint', fileContains(routesPath, 'close'));// ── 9. Routes Index ───────────────────────────────────────────────────────console.log('');console.log('── 9. Routes Index ───────────────────────────────────────────');const indexRoutesPath = path.join(ROOT, 'src', 'routes', 'index.js');check('routes/index.js exists', fileExists(indexRoutesPath));check('routes/index.js mounts inboundCustody routes', fileContains(indexRoutesPath, 'inboundCustody'));// ── 10. Frontend API ──────────────────────────────────────────────────────console.log('');console.log('── 10. Frontend API ──────────────────────────────────────────');const feApiPath = path.join(FRONTEND_ROOT, 'src', 'features', 'laundry-works', 'api', 'inboundCustodyApi.ts');check('frontend inboundCustodyApi.ts exists', fileExists(feApiPath));check('frontend API has getCustody', fileContains(feApiPath, 'getCustody'));check('frontend API has initiateCustody', fileContains(feApiPath, 'initiateCustody'));check('frontend API has confirmReceipt', fileContains(feApiPath, 'confirmReceipt'));check('frontend API has recordCountEvidence', fileContains(feApiPath, 'recordCountEvidence'));check('frontend API has closeCustody', fileContains(feApiPath, 'closeCustody'));// ── 11. Frontend Policy ───────────────────────────────────────────────────console.log('');console.log('── 11. Frontend Policy ───────────────────────────────────────');const fePolicyPath = path.join(FRONTEND_ROOT, 'src', 'features', 'laundry-works', 'policies', 'inboundCustody.policy.ts');check('frontend inboundCustody.policy.ts exists', fileExists(fePolicyPath));check('frontend policy has getInboundCustodyActionModel', fileContains(fePolicyPath, 'getInboundCustodyActionModel'));check('frontend policy has initiate action', fileContains(fePolicyPath, 'initiate'));check('frontend policy has confirmReceipt action', fileContains(fePolicyPath, 'confirmReceipt'));check('frontend policy has recordCountEvidence action', fileContains(fePolicyPath, 'recordCountEvidence'));check('frontend policy has close action', fileContains(fePolicyPath, 'close'));// ── 11b. Frontend Policy — Lifecycle Action Visibility ──────────────────────console.log('');console.log('── 11b. Policy Lifecycle Action Visibility ────────────────────');// Verify the policy enforces lifecycle-based action visibility// The confirmReceipt action must NOT be allowed when custodyStatus is null/undefined// (only initiate should be visible before any custody exists)const policyContent = fs.readFileSync(fePolicyPath, 'utf-8');const hasOldBug = policyContent.includes("!custodyStatus") && policyContent.includes("? allow('ยืนยันรับผ้า')");check('confirmReceipt denied when no custody (null) — old bug removed', !hasOldBug);check('confirmReceipt allowed only for PENDING', policyContent.includes("custodyStatus === 'PENDING'"));check('recordCountEvidence allowed only for RECEIPT_CONFIRMED', policyContent.includes("custodyStatus === 'RECEIPT_CONFIRMED'"));check('close allowed only for COUNT_EVIDENCE_RECORDED', policyContent.includes("custodyStatus === 'COUNT_EVIDENCE_RECORDED'"));check('initiate denied when custody exists (ALREADY_EXISTS)', policyContent.includes('ALREADY_EXISTS'));check('initiate allowed when no custody', policyContent.includes("? deny('เริ่มต้นการรับฝาก', 'ALREADY_EXISTS'"));check('confirmReceipt uses WRONG_STATUS deny for non-PENDING', policyContent.includes('WRONG_STATUS'));check('recordCountEvidence uses WRONG_STATUS deny for non-RECEIPT_CONFIRMED', policyContent.includes('WRONG_STATUS'));check('close uses WRONG_STATUS deny for non-COUNT_EVIDENCE_RECORDED', policyContent.includes('WRONG_STATUS'));check('no custody null-branch removed from confirmReceipt', !hasOldBug);// ── 12. Frontend Controller ───────────────────────────────────────────────console.log('');console.log('── 12. Frontend Controller ───────────────────────────────────');const feControllerPath = path.join(FRONTEND_ROOT, 'src', 'features', 'laundry-works', 'controllers', 'useInboundCustodyController.ts');check('frontend useInboundCustodyController.ts exists', fileExists(feControllerPath));check('frontend controller has initiate', fileContains(feControllerPath, 'initiate'));check('frontend controller has confirmReceipt', fileContains(feControllerPath, 'confirmReceipt'));check('frontend controller has recordCountEvidence', fileContains(feControllerPath, 'recordCountEvidence'));check('frontend controller has close', fileContains(feControllerPath, 'close'));// ── 13. Frontend Runtime Panel ────────────────────────────────────────────console.log('');console.log('── 13. Frontend Runtime Panel ────────────────────────────────');const fePanelPath = path.join(FRONTEND_ROOT, 'src', 'features', 'laundry-works', 'runtime', 'InboundCustodyRuntimePanel.tsx');check('frontend InboundCustodyRuntimePanel.tsx exists', fileExists(fePanelPath));check('frontend panel exports InboundCustodyRuntimePanel', fileContains(fePanelPath, 'InboundCustodyRuntimePanel'));check('frontend panel has initiate button', fileContains(fePanelPath, 'initiate'));check('frontend panel has confirmReceipt button', fileContains(fePanelPath, 'confirmReceipt'));check('frontend panel has recordCountEvidence input', fileContains(fePanelPath, 'recordCountEvidence'));check('frontend panel has close button', fileContains(fePanelPath, 'close'));// ── 14. Frontend Detail Page Integration ──────────────────────────────────console.log('');console.log('── 14. Frontend Detail Page Integration ──────────────────────');const feDetailPagePath = path.join(FRONTEND_ROOT, 'src', 'features', 'laundry-works', 'pages', 'LaundryWorkDetailPage.tsx');check('LaundryWorkDetailPage.tsx exists', fileExists(feDetailPagePath));check('detail page imports InboundCustodyRuntimePanel', fileContains(feDetailPagePath, "InboundCustodyRuntimePanel"));check('detail page renders InboundCustodyRuntimePanel', fileContains(feDetailPagePath, '<InboundCustodyRuntimePanel'));// ── Summary ───────────────────────────────────────────────────────────────console.log('');console.log('═══════════════════════════════════════════════════════════════');console.log(`  Results: ${passed} passed, ${failed} failed`);console.log('═══════════════════════════════════════════════════════════════');console.log('');if (failed > 0) {  process.exit(1);}