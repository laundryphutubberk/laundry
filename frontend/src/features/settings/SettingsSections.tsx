import{Link}from'react-router-dom';import type{WorkspaceSettings}from'./workspaceSettingsApi';export function MemberSummary({settings}:{settings:WorkspaceSettings}){return <section className="rounded-[24px] bg-white p-5 shadow-sm"><h2 className="text-xl font-black">สมาชิก Workspace</h2><p className="mt-1 text-sm text-slate-500">ข้อมูลสมาชิกปัจจุบันแบบอ่านอย่างเดียว</p><div className="mt-4 space-y-3">{settings.members.map(member=><article key={member.id} className="min-w-0 rounded-2xl border p-4"><p className="break-words font-black">{member.user.displayName||member.user.email}</p><p className="break-all text-sm text-slate-500">{member.user.email}</p><p className="mt-1 text-xs font-bold text-blue-700">{member.role}{member.positionTitle?` · ${member.positionTitle}`:''}</p></article>)}</div></section>};export function AccountAndAppInfo({settings}:{settings:WorkspaceSettings}){return <div className="space-y-5"><section className="rounded-[24px] bg-white p-5 shadow-sm"><h2 className="text-xl font-black">บัญชีและความปลอดภัย</h2><p className="mt-1 text-sm text-slate-500">จัดการรหัสผ่านและบัญชี Google ในหน้าความปลอดภัยเดิม</p><Link to="/workspace/laundry/security" className="mt-4 flex min-h-11 w-fit items-center rounded-2xl bg-blue-700 px-5 font-black text-white">เปิดการตั้งค่าความปลอดภัย</Link></section><section className="rounded-[24px] bg-white p-5 shadow-sm"><h2 className="text-xl font-black">ข้อมูลการใช้งาน</h2><dl className="mt-3 space-y-2 text-sm"><div><dt className="font-bold text-slate-500">บทบาทสมาชิก</dt><dd className="break-words font-black">{settings.currentMembership.role}</dd></div><div><dt className="font-bold text-slate-500">สถานะ Workspace</dt><dd className="font-black">{settings.workspace.status}</dd></div><div><dt className="font-bold text-slate-500">สาขาหลัก</dt><dd className="break-words font-black">{settings.branch?.name||'ไม่พบสาขาหลัก'}</dd></div></dl></section></div>}
