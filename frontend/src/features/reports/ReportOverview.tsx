import type {Overview} from './reportsApi'
const labels:Record<string,string>={totalWorks:'งานทั้งหมด',activeWorks:'งานกำลังดำเนินการ',closedWorks:'งานปิดแล้ว',cancelledWorks:'งานยกเลิก',readyWorks:'พร้อมส่ง',pendingWorks:'งานค้าง',totalResorts:'รีสอร์ต',totalBags:'ถุงทั้งหมด',totalItemCount:'จำนวนผ้าที่บันทึก',totalWeightKg:'น้ำหนักที่คำนวณได้ (กก.)',openIssues:'ปัญหาเปิด',reviewingIssues:'กำลังตรวจสอบ',resolvedIssues:'ปัญหาแก้ไขแล้ว',activeClaims:'เคลมที่กำลังดำเนินการ',totalClaims:'เคลมทั้งหมด'}
export function ReportOverview({data}:{data:Overview}){return <section><h2 className="text-xl font-black">ภาพรวมการดำเนินงาน</h2><div className="mt-3 grid grid-cols-2 gap-3 lg:grid-cols-4">{Object.entries(data.metrics).map(([key,value])=><article key={key} className="min-w-0 rounded-2xl bg-white p-4 shadow-sm"><p className="break-words text-xs font-bold text-slate-500">{labels[key]||key}</p><p className="mt-2 text-2xl font-black text-blue-800">{value.toLocaleString('th-TH',{maximumFractionDigits:3})}</p></article>)}</div></section>}
