# shared/types

Shared frontend type definitions.

Types should mirror backend API contracts and Prisma/domain language where appropriate.

Keep feature-specific types inside the owning feature package unless reused across multiple features.
