/**
 * App providers composition placeholder.
 *
 * FE-01 skeleton only. App-level providers belong here when introduced.
 */

export function AppProviders({ children }: { children: React.ReactNode }) {
  return children
}
